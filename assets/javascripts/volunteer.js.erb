(function() {
    Discourse.Dialect.inlineBetween({
        start: '[volunteer:sound]',
        stop:  '[/volunteer]',
        rawContents: true,
        emitter: function(contents) {
            return ['button', {
                'class' : 'btn-small volunteer-button',
                'show': contents,
                'volunteer-type': 'sound'
            }, '<i class="fa fa-pencil-square-o"></i>Volunteer'];
        }
    });
    Discourse.Dialect.inlineBetween({
        start: "[volunteer:trivia]",
        stop:  "[/volunteer]",
        rawContents: true,
        emitter: function(contents) {
            return ['button', {
                'class' : 'btn-small volunteer-button',
                'show': contents,
                'volunteer-type': 'trivia'
            }, '<i class="fa fa-pencil-square-o"></i>Volunteer'];
        }
    });
    Discourse.Markdown.whiteListTag('button', 'show', /^\d+$/);
    Discourse.Markdown.whiteListTag('button', 'volunteer-type','sound');
    Discourse.Markdown.whiteListTag('button', 'volunteer-type','trivia');
    Discourse.Markdown.whiteListTag('button', 'class','btn-small volunteer-button');

}).call(this);